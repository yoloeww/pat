#include<iostream>
#include<signal.h>

#define BLOCK_SIGNAL 2
using namespace std;
int main() {
    signala_t block,oblock;
    // 1.1 初始化
    sigemptyset(&block);
    sigemptyset(&oblock);

    sigprocmask(sig)
    return 0;
}
